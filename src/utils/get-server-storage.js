// export const getImageFile = (imageUrl) => {
//     return `${process.env.API_BASE_URL_STORAGE_LARAVEL}/${imageUrl}`;
// };

export const getImageFile = (imageUrl) => {
    return `https://be.gmooc4d.id/storage/${imageUrl}`;
};
